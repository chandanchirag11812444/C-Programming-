#include<iostream>
using namespace std;


int main()

{  
    int num1,num2;
    
    cout<<"Enter the value of num1 \n";/*this operator is known as insertion operator just insert and display in monitor*/
   cin>>num1;/*>>is operator is know as extraction operator mainly used to take value le lo like that in hindi*/
    cout<<"Enter the value of num2 \n";
    cin>>num2;
    cout<<"the sum is \n "<<num1+num2;
    return 0;
}
